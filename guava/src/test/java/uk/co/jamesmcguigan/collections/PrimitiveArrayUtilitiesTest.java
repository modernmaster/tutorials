package uk.co.jamesmcguigan.collections;

import org.junit.Test;

import java.util.Arrays;
import java.util.List;

public class PrimitiveArrayUtilitiesTest {


    @Test
    public void testCovertArrayToList() {

//        List list = Arrays.asList(1,2,3,4,5);
//        Collection.toArray(int[].class);
    }

    @Test
    public void testIndexOfArray() {
        List<Integer> list = Arrays.asList(1, 2, 3, 4, 5, 1);
        final Integer searchValue = 1;

//        assertThat(0, equalTo(Iterables.indexOf(list, input => input.contains(searchValue))));
//        assertThat(5, equalTo(Iterables.indexOf(list, input => input.contains(searchValue))));
//        int firstIndex = ;
//        int lastIndex = Iterables.indexOf(list, input => input.contains(searchValue));
//        DoubleFunction sine = (double x) -> Math.sin(x);

    }

    @Test
    public void testIndnosnod() {
//        Iterables.concat
//        Collections.min
//        Collections.max()
    }


}
