kafka:
  bootstrapAddress: kafka.local:9092
logging:
  level:
    root: ERROR
message:
  topic:
    name: testTopic
server:
  port: 8082
spring:
  data:
    rest:
      base-path: /api
      max-page-size: 5000
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    #password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
